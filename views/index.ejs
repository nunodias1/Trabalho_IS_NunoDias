<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Biblioteca de Livros</title>
  <style>
    /* Adiciona estilos CSS para o layout da grade */
    ul {
      list-style: none;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
    }

    li {
      text-align: center;
      width: 23%; /* Define a largura de cada livro para ocupar aproximadamente 1/4 da largura da tela */
      margin-bottom: 11%;
      margin-right: 5px;
      position: relative;
      border: 1px solid #87CEFA;
      padding: 10px ;
    }

    .book-container {
      position: absolute; /* Para posicionar absolutamente dentro de li */
      top: 100%;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      width: 100%;
      background-color: white;
      padding: 10px;
      box-sizing: border-box;
      border: 1px solid #87CEFA;
    }

    .book-info {
      text-align: left;
    }

    .actions {
      margin-top: 10px;
    }

    button {
      margin-left: 5px;
    }

  </style>

<form action="/pesquisar" method="post">
  <label for="termoPesquisa">Pesquisar:</label>
  <input type="text" id="termoPesquisa" name="termoPesquisa" required>
  <button type="submit">Pesquisar</button>
</form>

</head>
<body>
  <h1 style="text-align: center;">Biblioteca de Livros</h1>

  <form action="/adicionar" method="post" style="text-align: center;" enctype="multipart/form-data">
    <div style="display: inline-block; margin-right: 20px;">
      <label for="titulo" style="font-weight: bold;">Título</label>
      <br>
      <input type="text" id="titulo" name="titulo" required>
    </div>

    <div style="display: inline-block; margin-right: 20px;">
      <label for="autor" style="font-weight: bold;">Autor</label>
      <br>
      <input type="text" id="autor" name="autor" required>
    </div>

    <div style="display: inline-block;">
      <label for="anoPublicacao" style="font-weight: bold;">Ano de Publicação</label>
      <br>
      <input type="number" id="anoPublicacao" name="anoPublicacao" required>
    </div>

    <div style="margin-top: 10px;">
      <label for="capa" style="font-weight: bold;">Capa do Livro</label>
      <br>
      <br>
      <input type="file" id="capa" name="capa" accept="image/*">
    </div>

    <br>

    <button type="submit" style="border: 1px solid white; border-radius: 5px; background-color: #87CEFA; color: white; padding: 5px 235px; cursor: pointer; margin-top: 10px;">Adicionar Livro</button>
  </form>

  <br>

  <ul>
    <% livros.forEach((livro, index) => { %>
      <li>
        <!-- Exibir a capa do livro se estiver disponível -->
        <% if (livro.capa) { %>
          <img src="/uploads/<%= livro.capa %>" alt="Capa do Livro" style="width: 160px; height: 256px;">
          <div class="book-container">
            <p><strong>Título:</strong> <%= livro.titulo %></p>
            <p><strong>Autor:</strong> <%= livro.autor %></p>
            <p><strong>Ano de Publicação:</strong> <%= livro.anoPublicacao %></p>
            <div class="actions">
              <form action="/editar/<%= livro.id %>" method="get" style="display:inline;">
                <button type="submit">Editar</button>
              </form>
              <form action="/apagar/<%= livro.id %>" method="post" style="display:inline;">
                <button type="submit">Apagar</button>
              </form>
            </div>
          </div>
        <% } %>
      </li>
    <% }); %>
  </ul>
  
</body>
</html>

